#include<bits/stdc++.h>
using namespace std;
int main()
{
	string x,y;
	cin>>x;
	map<char,int> mp;
	for(int i=0;i<5;i++)
	{
		cin>>y;
		mp[y[0]]++;
		mp[y[1]]++;
	}
	if(mp[x[0]]>0||mp[x[1]]>0)
	cout<<"YES"<<endl;
	else
	cout<<"NO"<<endl;
}
