#include<bits/stdc++.h>
using namespace std;
typedef long long ll; 

int main()
{
	int k,n,w;
	cin>>k>>n>>w;
	ll sum=0;
	for(int i=1;i<=w;i++)
	{
		sum+=i*k;
	}
	if(n>=sum)
	cout<<0<<endl;
	else
	cout<<abs(sum-n)<<endl;
}
