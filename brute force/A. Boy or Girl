#include<bits/stdc++.h>
using namespace std;

int main()
{
	set<char> st;
	string s;
	cin>>s;
	for(auto  x: s)
	st.insert(x);
	if(st.size()%2==0)
	{
		cout<<"CHAT WITH HER!"<<endl;
	}
	else
	cout<<"IGNORE HIM!"<<endl;
}
