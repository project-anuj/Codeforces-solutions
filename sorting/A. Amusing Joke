#include<bits/stdc++.h>
using namespace std;
int main()
{
	string guest,host,s,s1="";
	cin>>guest>>host>>s;
	s1=s1+guest+host;
	sort(s1.begin(),s1.end());
	sort(s.begin(),s.end());
	if(s1==s)
	cout<<"YES"<<endl;
	else
	cout<<"NO"<<endl;
}
