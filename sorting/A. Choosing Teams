#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n,k;
	cin>>n>>k;
	int a[n];
	for(int i=0;i<n;i++)
	cin>>a[i];
	for(int i=0;i<n;i++)
	a[i]+=k;
	int cnt=0;
	for(int i=0;i<n;i++)
	if(a[i]<=5)
	cnt++;
	cout<<cnt/3<<endl;
}
