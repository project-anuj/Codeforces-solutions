#include<bits/stdc++.h>
using namespace std;
int main()
{
	vector<char> v;
	char x;
	map<char,int> mp;
	while(cin>>x)
	{	
		if(x=='}')
		break;
		else if(x!='{'&&x!=','&&x!=' ')
		{	if(mp[x]==0)
			{
				v.push_back(x);
				mp[x]++;
			}
		}
		
	}
	cout<<v.size()<<endl;
}
