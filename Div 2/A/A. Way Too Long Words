#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n;
	cin>>n;
	string s;
	while(n--)
	{
		cin>>s;
		if(s.length()>10)
		cout<<string(1,s[0])+to_string(s.length()-2)+string(1,s[s.length()-1])<<endl;
		else
		cout<<s<<endl;		
	}	
}
